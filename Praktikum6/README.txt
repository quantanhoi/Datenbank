For aufgabe4, we create a sql script for dropping 4 table which is created by the python script itself
The code is just a simple system call using linux terminal command
so if you are using window, it might not work, just copy the code from sql script and run it first before running the python script

